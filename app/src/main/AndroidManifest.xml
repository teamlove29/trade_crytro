<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alw.trade">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Trade">
        <activity
            android:name=".UI.WithdrawActivity"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".UI.HistoryActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".UI.MyWalletActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".UI.DepositActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".UI.CryptoActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".UI.OtherTokenActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>